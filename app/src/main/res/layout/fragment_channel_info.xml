<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <import type="pro.inmost.android.visario.ui.entities.channel.ChannelPrivacy" />
        <variable
            name="model"
            type="pro.inmost.android.visario.ui.entities.channel.ChannelUI" />
        <variable
            name="viewModel"
            type="pro.inmost.android.visario.ui.screens.channels.info.ChannelInfoViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/app_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:elevation="8dp"
            app:layout_constraintTop_toTopOf="parent">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
                app:layout_constraintTop_toTopOf="parent"
                app:navigationIcon="@drawable/ic_baseline_arrow_back_24"
                app:navigationIconTint="?attr/colorOnPrimary"
                app:popupTheme="@style/ThemeOverlay.AppCompat.DayNight"
                app:subtitle="@{model.privacy == ChannelPrivacy.PUBLIC ? @string/public_channel : @string/private_channel}"
                app:title="@{model.name}"
                app:titleTextColor="?attr/colorOnPrimary"
                tools:subtitle="Public channel"
                tools:title="Channel name">

            </com.google.android.material.appbar.MaterialToolbar>
        </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/channel_description_card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="0dp"
            app:layout_constraintTop_toBottomOf="@+id/app_bar">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="@string/description"
                    android:textColor="?attr/colorSecondaryVariant"
                    android:textSize="16sp"
                    android:textStyle="bold" />

                <com.google.android.material.textview.MaterialTextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="8dp"
                    android:ellipsize="end"
                    android:text="@{model.description.length() > 0 ? model.description : @string/no_description}"
                    android:textColor="?android:attr/textColorSecondary"
                    android:textSize="@dimen/text_size_chat_last_message"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/text_view_phone"
                    tools:text="This channel about some interesting things. Write me if you not agree with it." />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="0dp"
            android:onClick="@{() -> viewModel.onMembersClick()}"
            app:layout_constraintTop_toBottomOf="@id/channel_description_card"
            android:layout_marginTop="@dimen/margin_between_setting_cards">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="16dp"
                android:paddingEnd="22dp"
                android:paddingTop="16dp"
                android:paddingBottom="16dp"
                android:orientation="horizontal">

                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/tv_change_number"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="4"
                    android:textSize="@dimen/text_size_chat_name"
                    android:drawablePadding="16dp"
                    app:drawableTint="?attr/colorPrimary"
                    app:drawableStartCompat="@drawable/ic_baseline_people_24"
                    android:textColor="?android:attr/textColorPrimary"
                    android:text="@string/members"/>

                <com.google.android.material.textview.MaterialTextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textSize="@dimen/text_size_chat_name"
                    android:drawablePadding="8dp"
                    app:drawableTint="?attr/colorPrimary"
                    app:drawableEndCompat="@drawable/ic_arrow_right_24"
                    android:textColor="?android:attr/textColorPrimary"
                    android:text="@{String.valueOf(model.membersCount)}"
                    tools:text="666"/>

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>